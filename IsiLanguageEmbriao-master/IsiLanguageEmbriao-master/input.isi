programa

  numero a, c, d, e, f, g.
  texto b.

  e = 2.5

  escreva(a).
  b = 1.
  escreva(b).
  
  c = 2
  se ( b < c ){
       leia (d).     
  }
  senao{
	escreva (a).
  }

while (b != c){
	escreva(b).
	c = 1.
}

do {
	escreva(e).
	c = b.
} while (c != b).

escreva(g).

f = 1.

fimprog.